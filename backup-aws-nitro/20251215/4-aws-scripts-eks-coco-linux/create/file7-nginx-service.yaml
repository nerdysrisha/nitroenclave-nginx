apiVersion: v1
kind: Service
metadata:
  name: nginx-enclave-service
  namespace: integrations
spec:
  selector:
    app: nginx-enclave
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
  type: NodePort
