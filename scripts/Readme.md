The scripts placed here are versioned. 
These scripts are meant to spin up infrastructure where 'create' folder has all the creation scripts. 
The folder 'cleanup' has all the deletion scripts. 
From V2 onwards, a dedicated file added viz., 0-cleanup.sh. This file upon execution automatically executes all others and no need to individually execute. 
